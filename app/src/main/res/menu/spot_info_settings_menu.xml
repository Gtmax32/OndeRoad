<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <item android:id="@+id/reviewSubMenu"
        android:title="@string/review_settings"
        app:showAsAction="never"/>

    <item android:id="@+id/navigationSubMenu"
        android:title="@string/driver_navigation_settings"
        app:showAsAction="never"/>

    <item android:id="@+id/createSubMenu"
        android:title="@string/create_settings"
        app:showAsAction="never"/>
</menu>